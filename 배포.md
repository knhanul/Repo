개발 PC

1) ZIP 생성

cd C:\Pjt\Repo
.\scripts\make-deploy-zip.ps1

2) 서버 업로드

scp ".\dist\repo-update.zip" root@8.219.243.65:/tmp/repo-update.zip

Alibaba Cloud 서버

1) 운영 폴더 이동
ssh root@8.219.243.65

cd /opt/repo

2) 백업

TS=$(date +%Y%m%d-%H%M%S)

mkdir -p /opt/repo-backups

cp .env /opt/repo-backups/env-$TS
chmod 600 /opt/repo-backups/env-$TS

docker compose exec -T db \
  pg_dump -U nuni_repository nuni_repository \
  | gzip > /opt/repo-backups/db-$TS.sql.gz

tar \
  --exclude='.venv' \
  --exclude='.git' \
  --exclude='data' \
  -czf /opt/repo-backups/source-$TS.tar.gz \
  -C /opt/repo .

3) 새 ZIP 압축 해제

rm -rf /tmp/repo-update
mkdir -p /tmp/repo-update
unzip /tmp/repo-update.zip -d /tmp/repo-update

4) 소스 반영

rsync -a --delete \
  --exclude='.env' \
  --exclude='data/' \
  --exclude='.git/' \
  --exclude='.venv/' \
  /tmp/repo-update/ \
  /opt/repo/

5) 설정 검사

cd /opt/repo
docker compose config -q

6) Docker 재빌드

docker compose up -d --build

7) 상태 확인

docker compose ps

docker compose logs --tail=100 app

8) Health Check

curl -i http://127.0.0.1:8090/health

curl -i https://repo.nuni.co.kr/health

둘 다 정상이라면 배포 완료입니다.